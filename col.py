
def myzip(it1, it2):
    # Use zip to combine elements from both collections
    zipped = zip(it1, it2)

    # Convert the result to a list for easy inspection or further use
    result = list(zipped)

    return result